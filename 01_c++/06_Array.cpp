#include<bits/stdc++.h>
using namespace std;

int main()
{
    // Normal Integer to Stored Multiple data
    // int a, b, c, d;
    // cin >> a >> b >> c >> d;
    // cout << a << b << c << d;

    // Array
    int arr[10];
    cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4] >> arr[5];
    arr[4] += 10;
    arr[4] = 3.4;
    cout << arr[4];

    return 0;
}