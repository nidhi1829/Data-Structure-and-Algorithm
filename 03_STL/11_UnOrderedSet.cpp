#include<bits/stdc++.h>
using namespace std;

void explainUnOrderedSet(){

    //lower_bound and upper_bound function does not work,
    // rest all functions are same as above,
    // it does not stores in any particular order it has a better complexity
    // than set in most cases, except some when collision happens(it has randomize order)
    // UnOrderSet is store unique value but not store in sorted manner...
}

int main(){
    explainUnOrderedSet();

    return 0;
}