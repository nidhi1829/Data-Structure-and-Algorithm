#include<bits/stdc++.h>
using namespace std;

void explainMultiMap(){

    // Everything same as map, only it can store multiple keys only mpp[key] cannot be used here 
    // stores dupliacte keys and in a sorted manner
}

int main(){

    explainMultiMap();

    return 0;
}